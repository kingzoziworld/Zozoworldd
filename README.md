# Zozoworld — Static Site

Pure HTML/CSS/JS. No build step.

## Files
- `index.html` — markup + SEO meta
- `style.css` — design system (dark luxe + gold)
- `script.js` — vendors, categories, filtering
- `vercel.json` — clean URLs config

## Deploy on Vercel
1. Push this folder to a Git repo (or drag-drop in Vercel dashboard).
2. Framework Preset: **Other**.
3. Build Command: *(leave empty)*
4. Output Directory: *(leave empty / `.`)*
5. Deploy.

Or via CLI:
```
npm i -g vercel
vercel --prod
```
